#!groovy

properties([
  // H allow predefined but random minute see https://en.wikipedia.org/wiki/Cron#Non-standard_characters
  pipelineTriggers([cron('H 07 * * 1-5')]),
  parameters([
    choice(
      name: 'ENVIRONMENT',
      choices: ['aat', 'preview'],
      description: 'Environment to run the test.'
    ),
    string(
      name: 'PR_NUMBER',
      defaultValue: '',
      description: 'PR number (REQUIRED if environment is preview, leave blank otherwise).'
    ),
    booleanParam(
      name: 'CHROME_TESTS',
      defaultValue: true,
      description: 'Tick the checkbox to run E2E tests in Chrome.'
    ),
    booleanParam(
      name: 'FIREFOX_TESTS',
      defaultValue: false,
      description: 'Tick the checkbox to run E2E tests in Firefox.'
    ),
    booleanParam(
      name: 'WEBKIT_TESTS',
      defaultValue: false,
      description: 'Tick the checkbox to run E2E tests in Webkit.'
    ),
    booleanParam(
      name: 'ACCESSIBILITY_TESTS',
      defaultValue: false,
      description: 'Tick the checkbox to run Accessibility tests.'
    ),
    booleanParam(
      name: 'RUN_SAUCE_LABS',
      defaultValue: true,
      description: 'Tick to run Sauce Labs cross-browser tests (tunnel + yarn test:crossbrowser). Off by default; enable when manually triggering.'
    ),
    booleanParam(
      name: 'SAUCE_MAC15_CHROME',
      defaultValue: true,
      description: 'Sauce: macOS 15 — Chrome (when Run Sauce Labs is enabled).'
    ),
    booleanParam(
      name: 'SAUCE_MAC15_FIREFOX',
      defaultValue: true,
      description: 'Sauce: macOS 15 — Firefox (when Run Sauce Labs is enabled).'
    ),
    booleanParam(
      name: 'SAUCE_WIN11_CHROME',
      defaultValue: true,
      description: 'Sauce: Windows 11 — Chrome (when Run Sauce Labs is enabled).'
    ),
    booleanParam(
      name: 'SAUCE_WIN11_EDGE',
      defaultValue: true,
      description: 'Sauce: Windows 11 — Microsoft Edge (when Run Sauce Labs is enabled).'
    ),
    booleanParam(
      name: 'SAUCE_WIN11_FIREFOX',
      defaultValue: true,
      description: 'Sauce: Windows 11 — Firefox (when Run Sauce Labs is enabled).'
    ),
  ])
])

@Library("Infrastructure")

def type = "nodejs"
def product = "pcs"
def component = "frontend"

def yarnBuilder = new uk.gov.hmcts.contino.YarnBuilder(this)

static Map secret(String secretName, String envVariable) {
  [$class     : 'AzureKeyVaultSecret',
   secretType : 'Secret',
   name       : secretName,
   envVariable: envVariable
  ]
}

def secrets = [
  'pcs-${env}': [
    secret('pcs-frontend-idam-secret', 'PCS_FRONTEND_IDAM_SECRET'),
    secret('idam-pcs-user-password', 'IDAM_PCS_USER_PASSWORD'),
    secret('idam-pcs-user-password-b64', 'IDAM_PCS_USER_PASSWORD_B64'),
    secret('pcs-solicitor-automation-uid', 'PCS_SOLICITOR_AUTOMATION_UID')
  ]
]

withNightlyPipeline(type, product, component) {
  validateInputs()
  enableSlackNotifications('#pcs-tech')
  loadVaultSecrets(secrets)
  handleEnvironmentSetting()
  setFunctionalTestEnvVars()
  enableFortifyScan()
  if (params.RUN_SAUCE_LABS) {
    // One Infrastructure stage: yarn test:crossbrowser runs saucectl once per selected suite (sequential),
    // then generates allure-report (see src/test/ui/utils/scripts/sauce-crossbrowser-run.sh).
    def suites = selectedSauceSuites()
    env.SAUCE_SUITE_NAMES = suites.join(' ')
    enableCrossBrowserTest()
    publishSauceAllureReport()
  }

  afterAlways('fortify-scan') {
    steps.archiveArtifacts allowEmptyArchive: true, artifacts: '**/Fortify Scan/**/*'
    runE2ETests(yarnBuilder)
    if (params.ACCESSIBILITY_TESTS) {
      runAccessibilityTests(yarnBuilder)
    }
  }
}

/** Suite `name` values in .sauce/config-sauce-nightly.yml (checkbox order preserved). */
def selectedSauceSuites() {
  [
    params.SAUCE_MAC15_CHROME ? 'pcs-frontend-mac15-chrome' : null,
    params.SAUCE_MAC15_FIREFOX ? 'pcs-frontend-mac15-firefox' : null,
    params.SAUCE_WIN11_CHROME ? 'pcs-frontend-win11-chrome' : null,
    params.SAUCE_WIN11_EDGE ? 'pcs-frontend-win11-edge' : null,
    params.SAUCE_WIN11_FIREFOX ? 'pcs-frontend-win11-firefox' : null,
  ].findAll { it != null }
}

def validateInputs() {
  if (params.ENVIRONMENT == 'preview' && (!params.PR_NUMBER?.trim())) {
    error("Validation Failed: PR number is required when environment is 'preview'")
  }
  if (params.ENVIRONMENT == 'preview' && !params.PR_NUMBER.trim().matches("^\\d+\$")) {
    error("Validation Failed: PR number must contain only numerical values")
  }
  if (params.RUN_SAUCE_LABS && selectedSauceSuites().isEmpty()) {
    error('Validation Failed: When Run Sauce Labs is enabled, select at least one Sauce desktop browser checkbox.')
  }
}

def handleEnvironmentSetting() {
  env.TEST_URL = (params.ENVIRONMENT == 'preview')
    ? "https://pcs-frontend-pr-${params.PR_NUMBER}.preview.platform.hmcts.net"
    : "https://pcs.${params.ENVIRONMENT}.platform.hmcts.net"
}

def setFunctionalTestEnvVars() {
  env.PCS_API_URL = 'http://pcs-api-aat.service.core-compute-aat.internal'
  env.DATA_STORE_URL_BASE = 'http://ccd-data-store-api-aat.service.core-compute-aat.internal'
  env.SAUCE_GREP = env.SAUCE_GREP ?: '@crossbrowser'
}

def runE2ETests(yarnBuilder) {
  env.ENABLE_ALL_PAGE_FUNCTIONAL_TESTS = 'true'
  if (params.CHROME_TESTS) {
    runE2ETestsForBrowser(yarnBuilder, 'Chrome')
  }
  if (params.FIREFOX_TESTS) {
    runE2ETestsForBrowser(yarnBuilder, 'Firefox')
  }
  if (params.WEBKIT_TESTS) {
    runE2ETestsForBrowser(yarnBuilder, 'Safari')
  }
}

def runE2ETestsForBrowser(yarnBuilder, String browser) {
  stage("Full E2E Test - ${browser}") {
    node {
      checkout scm
      sh 'rm -rf allure-results allure-report'
      catchError(buildResult: 'UNSTABLE', stageResult: 'FAILURE') {
        yarnBuilder.yarn("test:E2e${browser}")
      }
      publishHTML([
        allowMissing: true,
        alwaysLinkToLastBuild: true,
        keepAll: true,
        reportDir: "allure-report",
        reportFiles: 'index.html',
        reportName: "Full ${browser} E2E Test Report"
      ])
      steps.archiveArtifacts(
        allowEmptyArchive: true,
        artifacts: 'definitions/test/playwright-report/**'
      )
      sendAllureReportToSlack(browser, "Full_20${browser}_20E2E_20Test_20Report/")
    }
  }
}

/** After enableCrossBrowserTest(): HTML report from src/test/ui/utils/scripts/sauce-crossbrowser-run.sh if present in workspace. */
def publishSauceAllureReport() {
  try {
    if (fileExists('allure-report/index.html')) {
      publishHTML([
        allowMissing         : true,
        alwaysLinkToLastBuild: true,
        keepAll              : true,
        reportDir            : 'allure-report',
        reportFiles          : 'index.html',
        reportName           : 'Sauce Labs Cross-Browser Allure Report'
      ])
    } else {
      echo 'Sauce Allure: allure-report/index.html not found; skipping HTML publisher.'
    }
    steps.archiveArtifacts(
      allowEmptyArchive: true,
      artifacts: 'allure-report/**/*,allure-results/**/*,allure-results-sauce-merge/**/*'
    )
  } catch (Exception e) {
    echo "WARNING: Sauce Allure publish/archive skipped: ${e.message}"
  }
}

def runAccessibilityTests(yarnBuilder) {
  stage("Full Accessibility Test") {
    node {
      checkout scm
      sh 'rm -rf allure-results allure-report'
      catchError(buildResult: 'UNSTABLE', stageResult: 'FAILURE') {
        yarnBuilder.yarn("test:accessibility")
      }
      publishHTML([
        allowMissing         : true,
        alwaysLinkToLastBuild: true,
        keepAll              : true,
        reportDir            : "allure-report",
        reportFiles          : 'index.html',
        reportName           : 'Accessibility Test Report'
      ])
      steps.archiveArtifacts allowEmptyArchive: true, artifacts: 'reports/tests-results/ManageCasesFunctional/*'
      sendAllureReportToSlack('Chrome', 'Accessibility_20Test_20Report/')
    }
  }
}

def sendAllureReportToSlack(String browser, String reportSuffix) {
  env.E2E_BROWSER = browser
  env.ALLURE_REPORT_PATH_SUFFIX = reportSuffix
  def msg = sh(script: "yarn exec ts-node src/test/ui/utils/common/allure-slack-notifier.ts --print-only --pipeline-type nightly 2>/dev/null || true", returnStdout: true).trim()
  if (!msg) { msg = "E2E Test Results — Build #${env.BUILD_NUMBER} — See ${env.BUILD_URL} for details" }
  try {
    slackSend(channel: '#hdp-qa-e2e-test-results', message: msg)
  } catch (Exception e) {
    echo "WARNING: Slack notification failed: ${e.message}"
  }
}
