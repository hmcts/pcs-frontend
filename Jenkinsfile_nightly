#!groovy

properties([
  // H allow predefined but random minute see https://en.wikipedia.org/wiki/Cron#Non-standard_characters
  pipelineTriggers([cron('H 07 * * 1-5')]),
  parameters([
    booleanParam(
      name: 'CHROME_TESTS',
      defaultValue: true,
      description: 'Desktop Chrome'
    ),
    booleanParam(
      name: 'FIREFOX_TESTS',
      defaultValue: false,
      description: 'Desktop Firefox'
    ),
    booleanParam(
      name: 'WEBKIT_TESTS',
      defaultValue: false,
      description: 'Desktop WebKit (Safari)'
    ),
    booleanParam(
      name: 'MICROSOFT_EDGE_TESTS',
      defaultValue: false,
      description: 'Microsoft Edge (desktop)'
    ),
    booleanParam(
      name: 'ENABLE_ALL_PAGE_FUNCTIONAL_TESTS',
      defaultValue: true,
      description: 'Enable page functional tests (content, errors, navigation) in Playwright.'
    ),
    choice(
      name: 'PLAYWRIGHT_GREP_TAG',
      choices: ['@nightly', '(all tests)', '@smoke', '@regression', '@accessibility'],
      description: 'Test title tag. Default @nightly. (all tests) = no tag filter.'
    ),
    string(
      name: 'PLAYWRIGHT_SPEC',
      defaultValue: '',
      description: 'Substring of a spec path (exact case). Leave blank for all specs. Invalid / no file match → all specs.'
    ),
  ])
])

@Library("Infrastructure")

def type = "nodejs"
def product = "pcs"
def component = "frontend"

def yarnBuilder = new uk.gov.hmcts.contino.YarnBuilder(this)

static Map secret(String secretName, String envVariable) {
  [$class     : 'AzureKeyVaultSecret',
   secretType : 'Secret',
   name       : secretName,
   envVariable: envVariable
  ]
}

def secrets = [
  'pcs-${env}': [
    secret('pcs-frontend-idam-secret', 'PCS_FRONTEND_IDAM_SECRET'),
    secret('idam-pcs-user-password', 'IDAM_PCS_USER_PASSWORD')
  ]
]

withNightlyPipeline(type, product, component) {
  enableSlackNotifications('#pcs-tech')
  loadVaultSecrets(secrets)
  handleEnvironmentSetting()
  setFunctionalTestEnvVars()
  enableFortifyScan()

  afterAlways('fortify-scan') {
    steps.archiveArtifacts allowEmptyArchive: true, artifacts: '**/Fortify Scan/**/*'
    runE2ETests(yarnBuilder)
  }
}

def handleEnvironmentSetting() {
  env.TEST_URL = 'https://pcs.aat.platform.hmcts.net/'
}

def setFunctionalTestEnvVars() {
  env.PCS_API_URL = 'http://pcs-api-aat.service.core-compute-aat.internal'
  env.DATA_STORE_URL_BASE = 'http://ccd-data-store-api-aat.service.core-compute-aat.internal'
  def grepChoice = params.PLAYWRIGHT_GREP_TAG ?: '@nightly'
  env.FUNCTIONAL_TEST_SCOPE = (grepChoice == '(all tests)') ? '' : grepChoice
  env.E2E_SPEC = params.PLAYWRIGHT_SPEC?.trim() ?: ''
  env.ENABLE_ALL_PAGE_FUNCTIONAL_TESTS = (params.ENABLE_ALL_PAGE_FUNCTIONAL_TESTS != false) ? 'true' : 'false'
}

def runE2ETests(yarnBuilder) {
  if (params.CHROME_TESTS) {
    runE2ETestsForProject(yarnBuilder, 'Chrome', 'Chrome')
  }
  if (params.FIREFOX_TESTS) {
    runE2ETestsForProject(yarnBuilder, 'Firefox', 'Firefox')
  }
  if (params.WEBKIT_TESTS) {
    runE2ETestsForProject(yarnBuilder, 'Safari', 'WebKit')
  }
  if (params.MICROSOFT_EDGE_TESTS) {
    runE2ETestsForProject(yarnBuilder, 'MicrosoftEdge', 'Microsoft Edge')
  }
}

def runE2ETestsForProject(yarnBuilder, String yarnScriptSuffix, String stageLabel) {
  stage("Full E2E Test - ${stageLabel}") {
    node {
      checkout scm
      sh 'rm -rf allure-results allure-report'
      catchError(buildResult: 'UNSTABLE', stageResult: 'FAILURE') {
        yarnBuilder.yarn("test:E2e${yarnScriptSuffix}")
      }
      publishHTML([
        allowMissing: true,
        alwaysLinkToLastBuild: true,
        keepAll: true,
        reportDir: "allure-report",
        reportFiles: 'index.html',
        reportName: "Full ${stageLabel} E2E Test Report"
      ])
      steps.archiveArtifacts(
        allowEmptyArchive: true,
        artifacts: 'definitions/test/playwright-report/**'
      )
      sendAllureReportToSlack(yarnScriptSuffix, "Full_20${yarnScriptSuffix}_20E2E_20Test_20Report/")
    }
  }
}

def sendAllureReportToSlack(String browser, String reportSuffix) {
  env.E2E_BROWSER = browser
  env.ALLURE_REPORT_PATH_SUFFIX = reportSuffix
  def msg = sh(script: "yarn exec ts-node src/test/ui/scripts/allure-slack-notifier.ts --print-only --pipeline-type nightly 2>/dev/null || true", returnStdout: true).trim()
  if (!msg) { msg = "E2E Test Results — Build #${env.BUILD_NUMBER} — See ${env.BUILD_URL} for details" }
  try {
    slackSend(channel: '#hdp-qa-e2e-test-results', message: msg)
  } catch (Exception e) {
    echo "WARNING: Slack notification failed: ${e.message}"
  }
}
