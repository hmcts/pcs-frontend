# Frontend preview against pcs-api PR 2738

Throwaway branch. Exists only so this PR's preview can be labelled
`pcs-api-pr:2738` and pointed at the HDPI-7333 pcs-api preview
(solicitor access-role cleanup / NoC).

Do not merge. Close when pcs-api#2738 merges.
