{# Tenancy Details template (matches GOV.UK layout shown) #}
{% extends "stepsTemplate.njk" %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/inset-text/macro.njk" import govukInsetText %}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}
{% from "macros/stepButtons.njk" import stepButtons %}
{% from "macros/csrf.njk" import csrfProtection %}

{% block pageTitle %}{{ pageTitle }} &ndash; HM Courts &amp; Tribunals Service &ndash; GOV.UK{% endblock %}

{% block mainContent %}
  {% if errorSummary %}
    {{ govukErrorSummary(errorSummary) }}
  {% endif %}
  {% if heading %}
    <h1 class="govuk-heading-l">{{ heading }}</h1>
  {% endif %}
  <form method="post" action="{{ url }}" novalidate>
    {% if tenancyDocument %}
      <div class="govuk-inset-text">
        <p class="govuk-body">{{ insetText }}</p>
        <p class="govuk-body">
          <a class="govuk-link" href="/case/{{ caseReference }}/view-documents/{{ tenancyDocument.id }}" target="_blank" rel="noopener noreferrer">{{ tenancyTypeDocumentLinkText }}</a>
        </p>
      </div>
    {% endif %}
      <p class="govuk-body govuk-!-font-weight-bold govuk-!-margin-bottom-2">{{ detailsHeading }}</p>
      <ul class="govuk-list govuk-list--bullet">
          <li>{{ tenancyType }}</li>
      </ul>
    {% for field in fields %}
      {% if field.componentType == 'radios' %}
        {{ govukRadios(field.component) }}
      {% endif %}
    {% endfor %}
    {{ stepButtons(saveAndContinue, saveForLater) }}
    {{ csrfProtection(csrfToken) }}
  </form>
{% endblock %}
